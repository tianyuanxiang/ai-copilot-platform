apiVersion: v1
kind: Service
metadata:
  name: postgresql-headless
  namespace: blade
  labels:
    app: postgresql

spec:
  clusterIP: None
  ports:
    - name: tcp-postgresql
      port: 5432
      targetPort: 5432
  selector:
    app: postgresql
