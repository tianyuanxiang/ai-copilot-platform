apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: blade
  labels:
    app: gateway
spec:
  type: ClusterIP
  ports:
    - port: 6666
      targetPort: 6666
      protocol: TCP
  selector:
    app: gateway
