apiVersion: v1
kind: Service
metadata:
  name: python-engine
  namespace: blade
  labels:
    app: python-engine
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
  selector:
    app: python-engine
