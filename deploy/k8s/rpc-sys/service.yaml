apiVersion: v1
kind: Service
metadata:
  name: sys-rpc
  namespace: blade
  labels:
    app: sys-rpc
spec:
  type: ClusterIP
  ports:
    - port: 9200
      targetPort: 9200
      protocol: TCP
  selector:
    app: sys-rpc
